WATCHTOWER_NOTIFICATIONS_HOSTNAME={{ ansible_host }}
SMTP_HOST=mail.lima-city.de
SMTP_PORT=465
SMTP_USERNAME=cyber@makerspace-gt.de
SMTP_PASSWORD={{ SMTP_PASSWORD_CYBER }}
SMTP_SENDER=noreply@makerspace-gt.de
SMTP_RECIPIENT=cyber@makerspace-gt.de
WATCHTOWER_NOTIFICATION_URL=smtp://${SMTP_USERNAME}:${SMTP_PASSWORD}@${SMTP_HOST}:${SMTP_PORT}/?fromaddress=${SMTP_SENDER}&toaddresses=${SMTP_RECIPIENT}
