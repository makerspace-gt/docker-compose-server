---

services:
  backup:
    volumes:
      - /opt/containers:/mnt/volumes/opt/containers
      - uptime-kuma_data:/mnt/volumes/uptime-kuma
      - signal-cli-rest-api_data:/mnt/volumes/signal-cli-rest-api

volumes:
  uptime-kuma_data:
    name: uptime-kuma_data
    external: true
  signal-cli-rest-api_data:
    name: signal-cli-rest-api_data
    external: true
